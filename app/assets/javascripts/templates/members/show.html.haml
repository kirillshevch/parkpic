.clearfix
  .clearfix
    %span(ng-repeat="photo in photos track by $index")
      .col-xs-3
        .text-line
          .userpic
            %img(ng-src="{{::photo.image_url}}" bs-modal template-url="photos/show_modal.html")
      .col-12(ng-if="($index + 1) % 4 == 0")

  .gutter-vertical-2x
    %pagination(page="page" per-page="25" items-count="count")
